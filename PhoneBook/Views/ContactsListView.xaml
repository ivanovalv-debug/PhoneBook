<UserControl x:Class="PhoneBook.Views.ContactsListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PhoneBook.ViewModels">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="DeepPink"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                CornerRadius="10" 
                                BorderBrush="Gray" 
                                BorderThickness="1" 
                                Background="{TemplateBinding Background}" 
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="Cornsilk"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                CornerRadius="10" 
                                BorderBrush="Gray" 
                                BorderThickness="1" 
                                Background="{TemplateBinding Background}" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.RowSpan="4" 
                Background="DeepPink" 
                CornerRadius="10" 
                Padding="10" 
                Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="Имя контакта:" FontWeight="Bold" Foreground="Cornsilk"/>
                <TextBox Grid.Row="1" 
                         Margin="0,5,0,10" 
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Background="Pink"/>

                <Label Grid.Row="2" Content="Номер телефона:" FontWeight="Bold" Foreground="Cornsilk"/>
                <TextBox Grid.Row="3" 
                         Margin="0,5,0,0" 
                         Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                         Background="Pink"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Добавить" Width="100" Margin="0,0,10,0" Height="30"
                    Command="{Binding AddCommand}" Background="LightCoral"/>
            <Button Content="Редактировать" Width="100" Margin="0,0,10,0" Height="30"
                    Command="{Binding EditCommand}" 
                    CommandParameter="{Binding SelectedContact}"
                    Background="#fdbdba"/>
            <Button Content="Удалить" Width="100" Margin="0,0,10,0" Height="30"
                    Command="{Binding DeleteCommand}" 
                    CommandParameter="{Binding SelectedContact}"
                    Background="Red"/>
            
        </StackPanel>

        <DataGrid Grid.Row="5" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  ItemsSource="{Binding Contacts}" 
                  SelectedItem="{Binding SelectedContact, Mode=TwoWay}"
                  SelectionMode="Single"
                  Background="Pink"
                  RowBackground="Pink"
                  AlternatingRowBackground="Pink"
                  BorderBrush="Gray"
                  BorderThickness="1"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  RowHeight="30">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="DeepPink"/>
                    <Setter Property="Foreground" Value="Cornsilk"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderBrush" Value="HotPink"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="DeepPink"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="5"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="HotPink"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Pink"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="LightPink"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="HotPink"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>