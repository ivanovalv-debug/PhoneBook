<UserControl x:Class="PhoneBook.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="30" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="📱 Телефонная книга" FontSize="28" FontWeight="Bold" Foreground="DeepPink" Margin="0,0,0,20" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding AppName}" FontSize="18" Margin="0,0,0,10" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding Version}" FontSize="16" Foreground="Gray" Margin="0,0,0,10" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding Description}" FontSize="14" TextWrapping="Wrap" HorizontalAlignment="Center" MaxWidth="400"/>
    </StackPanel>
</UserControl>